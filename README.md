# binary-to-decimal-app
A lightweight, responsive web app that converts binary strings up to 8 digits into their decimal equivalents. Built with HTML, CSS, and vanilla JavaScript, it features real-time input validation and utilizes manual positional base-2 math instead of built-in parsing shortcuts. Perfect for clean, modern browser-based calculations.
